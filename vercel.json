{
  "buildCommand": "pnpm --filter quirk-dashboard build",
  "outputDirectory": "apps/dashboard/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
